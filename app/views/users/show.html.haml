.page-header
  %h1
    .label.label-default= @position_name[@user.position]
    = @user.full_name

%p
  %b= 'Email: '
  = @user.email
%p
  %b= 'Miasto: '
  = @user.town.name

- if current_user.is_admin
  = link_to 'Edytuj', edit_user_path(@user), :class => 'btn btn-default'
= link_to 'Wstecz', users_path, :class => 'btn btn-default'
